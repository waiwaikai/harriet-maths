/// <reference types="vite/client" />
/// <reference types="vite-plugin-pwa/client" />

/** Build timestamp, injected at build time — shown in umma's corner and the progress summary. */
declare const __BUILD_ID__: string;
